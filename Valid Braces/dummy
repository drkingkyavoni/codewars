struct sym {
    char * type;
    int pos;
    int seq;
};


{[]({})}
    sym ('{', 0, 1);
    sym ('[', 1, 1);
    sym (']', 2, 1);
    sym ('(', 3, 1);
    sym ('{', 4, 2);
    sym ('}', 5, 2);
    sym (')', 6, 1);
    sym ('}', 7, 2);